<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>

  <ul>
    <% messages.forEach(message => { %>
      <li>
        <strong>User:</strong> <%= message.user %> <br>
        <strong>Message:</strong> <%= message.text %> <br>
      </li>
      <hr>
    <% }) %>
  </ul>
</body>
</html>
